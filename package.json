{
  "name": "notes-api",
  "version": "0.2.0",
  "private": true,
  "description": "A learning project for system design fundamentals.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}

